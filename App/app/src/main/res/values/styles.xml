<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="MyCheckboxStyle" parent="@android:style/Widget.CompoundButton.CheckBox">
        <item name="android:textColor">@color/white</item>
        <item name="hintTextColor">@color/white</item>
        <item name="colorControlActivated">@color/white</item>
        <item name="android:textColorPrimary">@color/white</item>
        <item name="suffixTextColor">@color/white</item>
        <item name="android:background">@color/text_sec</item>
        <item name="android:textSize">20dp</item>

        <!-- add any other desired properties here -->
    </style>
</resources>